package com.example.clase_vaadin;


/*import javax.servlet.annotation.WebServlet;

import com.vaadin.annotations.VaadinServletConfiguration;
import com.vaadin.navigator.Navigator;
import com.vaadin.server.VaadinRequest;
import com.vaadin.server.VaadinServlet;
import com.vaadin.ui.UI;

@SuppressWarnings("serial")
public class WindowNavegar extends UI{
	
	Navigator navigator;
	
	@WebServlet(value = "/*", asyncSupported = true)
	@VaadinServletConfiguration(productionMode = false, ui = WindowNavegar.class)
	public static class Servlet extends VaadinServlet {
	}

	@Override
	protected void init(VaadinRequest request) {
		getPage().setTitle("Ejemplo navigator");
		
		navigator = new Navigator(this,this);
		
		navigator.addView("StartView", new StartView());
		navigator.addView("OtraView", new OtraView());
	}

	
}
*/